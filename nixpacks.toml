[phases.setup]
nixPkgs = ['python39', 'ffmpeg', 'python39Packages.pip']

[phases.install]
cmds = ['pip3 install -r requirements.txt']

[start]
cmd = 'gunicorn app:app --bind 0.0.0.0:$PORT --timeout 600 --workers 2'
